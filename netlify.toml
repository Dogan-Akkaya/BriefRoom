[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA routing — any unknown path falls through to index.html so React Router handles it.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
